# Project Rosetta Board - Production Dashboard

This is the standalone crew dashboard deployed to Google Cloud Run, interfacing with the Vertex AI Agent Runtime instance.

### Runtime Environment Variables
- `GOOGLE_CLOUD_PROJECT`: The GCP project ID (e.g. `your-gcp-project-id`).
- `AGENT_RUNTIME_ID`: The full resource ID of the Vertex AI Reasoning Engine instance.
